replicaCount: 1

ingress:
  host: "api.dev.jaffleman.tech"


env:
  - name: PORT
    value: "80"
  - name: NODE_ENV
    value: "development"
  - name: LOG_LEVEL
    value: "debug"

resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits:   { cpu: 200m, memory: 256Mi }