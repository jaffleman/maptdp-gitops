replicaCount: 2

ingress:
  host: "api.jaffleman.tech"


env:
  - name: PORT
    value: "80"
  - name: NODE_ENV
    value: "staging"
  - name: LOG_LEVEL
    value: "info"

resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits:   { cpu: 300m, memory: 512Mi }
