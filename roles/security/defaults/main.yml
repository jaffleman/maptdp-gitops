ssh_user: ubuntu
ssh_port: 63210

ssh_pubkey_path: "{{ lookup('env','HOME') + '/.ssh/ansible_ed25519.pub' }}"

ufw_allowed_ports:
  - { port: 80, proto: tcp }
  - { port: 443, proto: tcp }
  - { port: 22, proto: tcp }