apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-app-maptdp-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/jaffleman/maptdp-gitops.git'
    path: maptdp-apps-staging
    targetRevision: staging

  destination:
    server: 'https://kubernetes.default.svc'
    namespace: rootapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true